#ifndef COMMAND_H
#define COMMAND_H
#include <stdint.h>

class command{
public:
	command(uint8_t correction): m_correction(correction){}

	const uint8_t getСorrectiono() noexcept
	{
		return m_correction;
	}

private:
	uint8_t m_correction;
};

#endif // COMMAND_H